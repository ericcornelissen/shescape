#!/bin/sh
. "$(dirname $0)/_/husky.sh"
. "$(dirname $0)/common.sh"

# Pre
if [ ! $IS_MERGING ]; then
  git stash push --include-untracked --quiet
fi

# Checks
npm run lint
npm run test
npm run transpile

# Post
if [ $(DID_STASH) ]; then
  git stash pop --quiet
fi
