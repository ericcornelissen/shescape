#!/bin/sh
. "$(dirname $0)/_/husky.sh"
. "$(dirname $0)/common.sh"

# Pre
if [ ! $(IS_MERGING) ]; then
  git stash push --include-untracked --keep-index --quiet
fi

# Checks
npm run format
git update-index --again

# Post
if [ $(DID_STASH) ]; then
  git stash pop --quiet
fi
