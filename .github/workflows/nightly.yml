name: Nightly
on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

permissions: read-all

jobs:
  audit:
    name: Audit (${{ matrix.ref }})
    uses: ericcornelissen/shescape/.github/workflows/reusable-audit.yml@revamp-ci
    with:
      refs: '["main"]'
  fuzz:
    name: Fuzz
    uses: ericcornelissen/shescape/.github/workflows/reusable-fuzz.yml@revamp-ci
    with:
      duration: 20m
