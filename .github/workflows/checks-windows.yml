name: Windows
on:
  push:
    paths:
      - ./src/win.js

permissions: read-all

jobs:
  fuzz:
    name: Fuzz
    uses: ericcornelissen/shescape/.github/workflows/reusable-fuzz.yml@only-fuzz-when-necessary
    with:
      duration: 5m
      platform: windows
